# Load your docker-compose.yml
docker_compose('./docker-compose.yml')

dc_resource('app',
  labels=['app'],
#   ports=['3000'],
)

dc_resource('ai',
  labels=['ai'],
#   ports=['3001'],
)

# dc_resource('redis',
#   labels=['redis'],
# )
